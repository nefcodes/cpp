#pragma once

class car {
private: 
	float fuel;
	float speed;
	int passengers;

public:
	//creating a default constructor
	car();
	~car(); // this is a destructor which is called by default after the instance is destroyed.

	void fillfuel(float amount); // You could have declared function here or outside the class
	void accelerate();
	void brake();
	void addpassengers(int count);
	void dashboard();
};

// Now you need to define these functions in a separate .cpp file